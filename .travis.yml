language: python
python:
- "3.9"

install: "pip install -r requirements.txt"

script:
- pytest appTest.py -v --cov

notifications:
 email:
  recipients: 
   -lzhang200@bcit.ca
  on_success: always # default: change
  on_failure: always
